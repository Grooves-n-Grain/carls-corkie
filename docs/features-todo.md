features-todo:

Project board:
- project folder, or tagging so you can keep all related projects together.
- change the `New Project` modal to have the "New Projext" text be the same as the cards below it.
- celebration mode that activates when you fully complete a project. TBD. 
- make the `+ New` button in the top header more vibrant and noticeable.
- can you think of any actionable things could we have that make sense in the spirit of the project.
- coding tracker integrated with Claude Code via hooks or something. e.g. Before coding we create a project card with all the deliveribles, then check em off after each task is complete. How do we make this a consistant part of the workflow, and not something we do only when we remember?

Pin board:
- actionable items. e.g. If a email is posted about a followed required, you can click the link on the card and it will inject the context into your agents chat so action can be taken. (more...the card context would be provided to your agents, i.e. openclaw, hermes-agent, etc.
- `Event` card text overflows the card boundries. Do other cards as well? I've only noticed it on the Event card so far.
- associate lamp colours with cards so that it actully lights up when a new card arrives.
- define `Focus` mode and how it works, along with the card priority.
- actionable links. e.g. if a card references a link, then you should be able to click the link on the card and have it open.
- what other actionable things could we have that make sense in the spirit of the project.
